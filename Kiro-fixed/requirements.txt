# ============ 核心依赖 ============
# OpenAI 兼容客户端（调用 vLLM 部署的 DeepSeek 和 BGE）
openai>=1.40.0

# ============ 数据存储 ============
mysql-connector-python>=9.0.0
pymilvus>=2.5.0,<2.6.0

# ============ LangGraph（后续迁移用，当前用纯Python实现） ============
langgraph>=0.3.0
langchain>=0.3.0

# ============ API 服务 ============
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0

# ============ 可视化界面 ============
gradio>=4.0.0

# ============ 工具 ============
python-dotenv>=1.0.0
