"""
LLM Prompt 模板
"""

# ============================================================
# 意图识别
# ============================================================

INTENT_PROMPT = """你是一个意图识别器，判断用户问题的类型。

## 当前对话上下文
最近的话题模块: {recent_modules}
用户新问题: {user_query}

## 意图类型
1. knowledge_query: 需要查找专业知识的问题（如"析锂是什么"、"正极材料有哪些"）
2. followup_query: 对之前对话内容的追问/延伸（如"那另一个呢"、"详细说说"）
3. chitchat: 闲聊/问候/与知识库无关的问题（如"你好"、"今天吃什么"）

思考完成后，最后一行只输出: knowledge_query / followup_query / chitchat"""

# ============================================================
# 话题判断：新问题属于当前模块还是新模块
# ============================================================

TOPIC_JUDGE_PROMPT = """判断用户的新问题是否属于当前正在讨论的话题模块。

## 当前话题模块
话题: {current_topic}
模块摘要: {current_summary}

## 用户新问题
{user_query}

## 判断规则（适度判定）
- same: 以下情况算 same：
  1. 新问题跟当前话题是同一个具体主题（如都在讨论"析锂"）
  2. 新问题是对当前话题的直接追问或延伸（如"怎么防止？""还有什么原因？"）
  3. 新问题涉及的核心概念在当前模块摘要中明确出现过

- new: 以下情况算 new：
  1. 新问题讨论的是一个不同的具体主题（即使同属锂电池大领域）
  2. 新问题的核心概念在当前模块中没有出现过
  
  例如：当前聊"析锂" → 新问题"磷酸铁锂和三元材料哪个安全" → new（虽然都是锂电池，但具体主题不同）
  例如：当前聊"析锂原因" → 新问题"怎么防止析锂" → same（同一主题的延伸）
  例如：当前聊"正极材料对比" → 新问题"三元材料能量密度" → same（在对比范围内的追问）

思考完成后，最后一行只输出: same 或 new"""

# ============================================================
# 模块摘要生成
# ============================================================

MODULE_SUMMARY_PROMPT = """根据以下对话内容，生成这个话题模块的主题名称和摘要。

## 模块中的对话
{conversations}

## 输出格式（严格JSON）
{{"topic": "话题名称（2-8字，如：析锂、正极材料、热失控）", "summary": "模块摘要（描述这个模块讨论了什么内容，50-150字）"}}

注意：只返回JSON。"""

# ============================================================
# 问题改写
# ============================================================

QUERY_REWRITE_PROMPT = """根据对话上下文改写用户的问题，使其更完整、更适合检索。

## 对话上下文（最近几轮）
{context}

## 用户新问题
{user_query}

## 要求
- 如果问题有指代（如"那个"、"它"），补全指代
- 如果问题太简短，根据上下文补充关键词
- 如果问题已经足够清晰，直接返回原问题
- 只返回改写后的问题，不要其他文字"""

# ============================================================
# 答案聚合
# ============================================================

ANSWER_AGGREGATE_PROMPT = """根据以下信息回答用户的问题。

## 来自历史对话记忆（Wiki）
{wiki_context}

## 来自知识库检索（RAG）
{rag_context}

## 用户问题
{user_query}

## 要求
1. 综合 Wiki 和 RAG 的信息回答
2. 如果 Wiki 中已有完整答案，优先使用
3. 用 RAG 的信息补充 Wiki 中没有的内容
4. 回答要准确、有条理、使用中文
5. 如果信息不足，可以用你自己的知识补充，但标明"""

ANSWER_DIRECT_PROMPT = """直接回答用户的问题。

## 用户问题
{user_query}

## 要求
1. 如果是闲聊/问候，自然回应
2. 如果是知识性问题，基于你的知识回答
3. 使用中文，简洁有条理"""
